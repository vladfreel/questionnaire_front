.container
  %h1
    Questions
  %br
  %br
  .questions
    - if @questions.present?
      - @questions.each do |question|
        .question
          = question['content']
          .answers
            - question['answers'].each do |answer|
              = answer['content']